# StudyGenius

Monorepo for the StudyGenius learning app built with Turborepo and pnpm.

## Development

- `pnpm dev` – run all apps in development.
- `pnpm build` – build all packages and apps.
- `pnpm lint` – lint all sources.
- `pnpm test` – run tests.
